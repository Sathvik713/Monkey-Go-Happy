var bananaImage;
var obstacleImage, obstacleGroup;
var background;

var score;

function preload()
background = Image("jungle.jpg");

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
}